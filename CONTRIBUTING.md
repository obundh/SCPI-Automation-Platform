# 기여 안내

이 프로젝트는 안전한 계측 자동화와 출처를 확인할 수 있는 장비 프로필을
목표로 한다. 버그 수정, 테스트, 초보자용 문구와 검증 가능한 프로필 개선을
환영한다.

## 제출 전에 확인할 사항

1. 변경 후 다음 검사를 실행한다.

   ```powershell
   $env:PYTHONPATH = "src"
   python -m unittest discover -s tests -v
   python .\run_app.py --smoke-test
   python .\tools\check_public_release.py --skip-history
   ```

2. 제조사 매뉴얼 원문·PDF·표·그림·화면 캡처, OCR 텍스트, 전체 명령 색인,
   명령과 페이지의 대응표를 커밋하지 않는다.
3. 실제 SCPI 바인딩을 추가할 때는 permissive 라이선스가 확인된 정확한
   소스 경로와 revision, 독립적인 실장비 검증 또는 제조사의 명시적 재사용
   허가 중 하나를 기록한다. 단순히 매뉴얼에서 보았다는 사실만으로 공개
   프로필에 명령을 추가하지 않는다.
4. 실장비 검증 결과에는 실제 일련번호·사내 IP·고객명·시험 대상의 비공개
   정보를 넣지 않는다.
5. RF 출력, 전압, 전류, 전력, calibration, reset 등 상태를 바꾸는 기능에는
   범위 제한, 원복 또는 안전 종료, 오류 확인과 테스트를 함께 추가한다.

## 기여 라이선스

별도 표시가 없는 한 제출한 기여는 저장소의
[MIT License](LICENSE) 조건으로 제공하는 데 동의한 것으로 본다. 본인이
제출할 권리가 없는 소속기관 코드, 비공개 코드 또는 제3자 자료를 보내지
말아야 한다.

제조사명과 모델명은 호환성 식별에 필요한 범위에서만 사용한다. 제조사
로고, 제품 사진, 고유 UI 복제물과 마케팅 이미지는 제출하지 않는다.
