# 장비 조작기 V1 Tkinter GUI 설계안

작성일: 2026-07-25  
상태: 검색·다중 장비 선택·루틴·계획·실제 실행·결과 확인의 5단계 UI 구현 반영  
범위: 장비 검색·검증·루틴 편집·계획 snapshot·Dry Run/실제 실행·안전 종료·MD/JSON/Excel 결과 저장

## 1. GUI 기술 결정

V1은 **Tkinter + ttk**로 만든다.

선택 이유:

- Python 기본 GUI라 배포 의존성이 작다.
- 구형 Windows 시험 PC에서 비교적 가볍다.
- 인터넷 없는 PyInstaller one-folder 배포에 적합하다.
- 현재 `SEreport`와 조작 감각이 이어진다.
- 계측기 전면 패널처럼 정보가 조밀한 화면을 만들기 쉽다.
- 복잡한 계획 편집기는 별도이므로 Tkinter의 drag-and-drop 한계가 V1에 문제가 되지 않는다.

초기에는 CustomTkinter, Qt, Electron과 브라우저 UI를 도입하지 않는다. 스타일은 기본 `ttk` 위에서 제한적으로 조정한다.

### 카탈로그 수량을 화면에 표현하는 원칙

현재 배포 데이터 계층은 permissive 오픈소스를 근거로 한 12개 프로필,
237개 capability와 390개 구조화된 operation으로 구성된다. 제조사 매뉴얼
원문·명령 색인·페이지 매핑은 프로그램, 저장소와 배포물에 포함하지 않는다.
공식 문서 링크는 문서명·버전 등 서지정보를 확인하기 위한 참조일 뿐이다.

GUI는 배포 프로필의 capability 수와 현재 장비에서 검증을 통과한 operation
수를 구분한다. permissive 소스에 존재한다는 사실이나 Query 응답 수신만으로
기능 검증 `pass`가 되지 않는다. 파라미터·응답 파서·위험도·복원 절차를
구조화하고 해당 물리 장비에서 operation 검증을 통과한 항목만 실행 기능으로
표시한다.

`내 로컬 명령 후보` 탭의 `선택 후보를 기능으로 검증·등록`은 별도 편집창을
연다. 이 탭은 사용자가 적법하게 사용할 수 있는 매뉴얼로 저장소 밖에서 직접
만든 비공개 후보만 읽는다. 원문·OCR·명령 색인·페이지 매핑은 Git이나
실행파일에 포함하지 않는다.
사용자는 조회·설정·실행 종류, 화면명, 명령 template, paired Query, 응답형,
시험값, 파라미터 타입·단위·범위·선택값과 위험도를 확인한다. 저장 버튼은
기능을 바로 활성화하지 않고 실장비 검증을 시작한다. IDN·옵션과 출처가
일치하고 모든 operation이 PASS일 때만 로컬 기능으로 표시한다.

로컬 기능은 제조사·모델·시리얼·펌웨어·옵션과 검증 증거에 묶는다. 다른 장비의
기록, 정의·범위가 바뀐 기록, 재검증 실패 기록은 루틴에 보이지 않게 한다.
로컬 SET/EXECUTE는 항상 고위험 승인 대상으로 두고, `manual_only` 후보는 자동
Query로 전환하지 않는다. 옵션은 조회됨·미지원·미조회로 구분해 live 상태를
다시 확인한다. 최초 로컬 기능은 검증 때 사용한 값·채널·Trace·선택지 조합만
보이며 다른 조합은 별도 검증을 요구한다. 레지스트리 JSON은 HMAC과 Windows
사용자 DPAPI 키가 일치하지 않으면 읽지 않는다. JSON·anti-rollback state·
DPAPI key anchor의 세대와 digest도 모두 일치해야 한다. 세 파일 전체를 같은
과거 백업으로 복원한 경우는 외부 단조 카운터 없이 판별할 수 없으므로 재검증
안내를 표시한다.
여러 창이 열려 있으면 레지스트리 generation/digest를 저장 직전에 비교한다.
다른 창이 먼저 변경한 경우 현재 저장을 거부하고 최신 목록을 다시 열도록
안내한다.
`fully_resolved`는 배포 프로필의 operation 범위 상태일 뿐 제조사 매뉴얼
전체 명령의 완전 지원을 뜻하지 않으므로 최종 카드는 `부분 검증` 표현을
유지한다.
장비 분류별 공통 기능 카드는 초보자 설명과 데모용이다. 실제 장비의 최종
분류·루틴에서는 capability/operation ID와 실장비 PASS가 있는 모델 기능만
추가할 수 있다. 저장 파일의 상태나 allowlist는 이전 스키마를 포함해 다시
권한으로 사용하지 않는다.

## 2. 화면의 성격

첫 연결 과정은 최근 금융·생활 앱처럼 **한 번에 한 단계만 설명하는 안내형 화면**으로 만든다. 장비가 연결된 뒤의 조작 화면은 값과 상태를 빠르게 읽을 수 있는 클래식 계측기 패널 성격을 유지한다.

초보자 안내 원칙:

- `VISA resource 검색` 대신 `연결된 장비 찾아보기`처럼 해야 할 행동을 먼저 말한다.
- VISA는 `PC와 장비가 대화하는 공용 통로`, IDN은 `장비가 건네는 이름표`라고 설명한다.
- `장비 찾기 → 연결 확인 → 분류 완료`의 현재 단계를 항상 표시한다.
- 성공하면 `장비를 분류했어요!`, 실패하면 `괜찮아요. 이렇게 해보세요`로 다음 행동을 안내한다.
- backend, timeout, resource 주소와 raw IDN은 `고급 설정`과 `자세히 보기` 안에 둔다.
- 기술 오류를 그대로 던지지 않고 쉬운 제목, 짧은 설명, 해결 순서와 기술 상세를 구분한다.
- 사용자가 입력한 IDN, 배포 프로필 선택과 operation별 실장비 검증 결과를
  서로 다른 단계와 상태로 표시한다.

분류 완료 화면은 같은 종류의 장비를 하나의 큰 분류 카드로 묶는다. 카드 상단에는 창 크기에 맞춰 다시 그려지는 분류별 벡터 이미지, `스펙트럼 분석기` 같은 분류명과 쉬운 설명을 배치한다. 그 아래에는 세로 연결선과 점을 따라 실제 장비의 제조사·모델명, 용도 설명과 식별 상태를 이어서 표시해 `장비 종류 → 발견된 실제 장비` 관계를 초보자도 바로 이해하게 한다.

각 실제 장비 카드 오른쪽에는 `이 장비 사용` 체크박스를 둔다. 여러 장비를 먼저 고를 수 있도록 체크와 동시에 화면을 바꾸지 않고, 한 대 이상 선택하면 활성화되는 `선택한 장비 N대로 루틴 만들기` 버튼을 눌렀을 때 루틴 설정 탭으로 이동한다.

장비 조작 화면 원칙:

- 값, 단위, 실제 장비 readback을 한 화면에서 비교할 수 있어야 한다.
- 텍스트를 입력하는 것만으로는 장비에 전송되지 않는다.
- 모든 설정에는 명시적인 `적용` 버튼이 있다.
- 가능한 설정에는 `읽기` 버튼과 현재 장비값 표시가 있다.
- SCPI 문자열은 기본 화면에 노출하지 않는다.
- 연결, 작업 중, 출력 ON, 오류를 색상뿐 아니라 텍스트로도 표시한다.
- 위험 기능은 일반 설정 버튼과 시각적으로 분리한다.

## 3. 현재 5단계 흐름과 후속 직접 조작 화면

### 현재 구현한 첫 탭

```text
장비 찾기
  “VISA는 PC와 장비가 대화하는 통로예요.”
  “IDN은 장비가 보내는 이름표예요.”
  [연결된 장비 찾아보기]
          ↓
연결 확인
  검색 중 진행 표시
  장비 설정 명령은 보내지 않는다는 안내
          ↓
분류 완료
  장비별 결과 카드
  정확 IDN 후보 / 모델군 추정 / 확인 필요 구분
  [배포 프로필 선택 또는 확인]
          ↓
기능 검증
  전체 operation의 통과 / 실패 / 미확인 / 위험 / 수동 확인
  [안전 조회 검증] [복원 가능한 설정 검증] [중지]
          ↓
최종 분류
  이 장비에서 통과한 기능만 사용 가능
  [□ 이 장비 사용] 여러 대 선택
  [자세히 보기]
  [선택한 장비 N대로 루틴 만들기]
```

장비가 보이지 않을 때는 같은 결과 화면에서 전원, 케이블, VISA 드라이버, 다른 제어 프로그램 점유와 주소 직접 입력을 순서대로 안내한다. 오류 팝업만 보여 주고 끝내지 않는다.

IDN이 카탈로그 모델과 정확히 일치해도 기능 검증을 건너뛰지 않는다. 일치
결과는 배포 프로필을 자동 선택하는 근거일 뿐이다. 일치하지 않는 장비는
같은 장비 분류의 프로필을 기준으로 고를 수 있지만, 그 프로필의 기능을
자동으로 상속하지 않는다.

검증 화면은 배포 프로필의 OSS 근거 기능, 사용자 로컬 후보, 실제로 시험한
결과와 최종 사용 가능 기능을 구분한다. read-only query를 먼저 수행하고,
복원 가능한 설정은 현재값 저장·시험값 적용·readback·원래 값 복원을 한
묶음으로 표시한다.
출력, 파일·메모리 변경, reset, calibration 등은 일반 검증 버튼에서 제외하고
물리 조건을 확인하는 수동 승인 단계로 보낸다.

### 현재 구현한 루틴 설정 탭

```text
선택한 장비로 루틴을 만들어볼게요
  장비 [선택한 장비 ▼]

  사용 가능한 기능       공통 단계                  내 루틴
  [기능 Listbox]          Delay - 대기 시간          [불러오기] [루틴 저장]
  [루틴에 추가 →]         [시간(초)] [대기 추가]     [실행 순서 Listbox]
                         Wait for Completion         [위로] [아래로] [삭제]
                         [대상 장비] [제한 시간]     [전체 비우기]
                         [완료 확인 추가]
                                                    [루틴 저장 및 다음 단계 →]
```

- 장비 Combobox에서 선택한 장비를 바꾸면 해당 장비의 기능 목록을 표시한다.
- 기능 목록에는 배포 프로필 전체가 아니라 해당 물리 장비에서 `pass`가 된
  operation만 표시한다.
- 기능을 골라 내 루틴에 추가하고 위·아래 이동, 삭제와 전체 비우기로 순서를 편집한다.
- `Delay`는 장비 명령이 아니라 현재 PC 실행기가 지정 시간 동안 쉬는 장비 독립 공통 단계다.
- `Wait for Completion`은 완료를 확인할 대상 장비와 유한한 제한 시간을 반드시 함께 저장한다.
- 실제 완료 확인 전략은 배포 프로필의 `*OPC?`, `*OPC`와 상태 폴링, 모델
  전용 상태 조회 중 그 장비에서 검증된 방법으로 연결한다. 검증된 전략이
  없는 장비에서는 실행을 거부하며 `Delay`로 자동 대체하지 않는다.
- 내 루틴의 각 항목에는 대상 장비 이름을 함께 표시해 여러 장비를 구분한다.
- 루틴은 UTF-8 `*.scpiroutine.json` schema v5로 저장한다. 파일에는 버전,
  필요한 장비의 raw IDN·식별정보·옵션 상태, 배포 프로필 ID,
  operation별 통과·실패·미확인
  목록, 기능 ID와 공통 단계 값만 넣고 raw SCPI 문자열은 넣지 않는다.
- 파일 안 allowlist는 권한으로 신뢰하지 않고 현재 선택 장비의 검증 기록과
  다시 결합한 뒤에만 기능 단계를 복원한다.
- `루틴 저장 및 다음 단계`는 저장 성공 후에만 계획서 탭으로 이동한다. 파일 선택 취소나 저장 오류에서는 현재 탭을 유지한다.
- 불러올 때 저장 당시 장비를 현재 선택 장비로 다시 확인한다. resource가 같고 식별정보 충돌이 없거나, manufacturer·model·serial·분류와 profile이 정확히 같은 후보가 한 대일 때만 자동 연결한다.
- 필요한 장비가 하나라도 없거나 모호하면 루틴을 일부만 적용하지 않고 현재 작성 내용을 그대로 유지한 채 누락 장비를 안내한다.
- 루틴 항목을 우클릭하면 복제, 한 단계 위·아래 이동, 맨 위·아래 이동과 삭제를 할 수 있다. 빈 공간 우클릭은 마지막 항목을 잘못 선택하지 않는다.
- 장비를 선택하지 않은 상태에서는 장비 찾기 탭에서 먼저 고르도록 안내한다.
- 루틴 편집 단계에서는 실제 SCPI를 전송하지 않는다. 분류 단계의 검증
  세션에서 확인한 operation과 안전 한계만 이후 실행 엔진에 연결한다.

### 현재 구현한 계획서 탭

```text
장비별 측정 계획을 만들어볼게요

  1. 시험 설정 입력                  2. 만들어진 계획
  사용 장비 [FSV30/SMB100A ▼]       [시험 조건 Listbox]
  Center - 중심 주파수 [1] [GHz]    [위로] [아래로] [삭제]
  Span - 주파수 분석 범위 [100]     [전체 비우기]
  RBW - 분해능 대역폭 [100] [kHz]
  VBW - 비디오 대역폭 [자동]
  Ref. Level - 화면 기준 레벨 [0]
  결과: Peak Search 후 Marker 값
  [계획 상세 설정] [현재 설정 1개 추가 →]

  신호발생기를 선택하면:
  Frequency - 출력 주파수
  Power - 출력 설정값
  Dwell - 주파수 유지 시간
```

- 첫 두 장비 분류로 스펙트럼 분석기와 신호발생기를 지원한다.
- 장비 찾기에서 선택한 두 분류의 장비를 사용 장비 목록에 표시하고, 선택 분류에 맞는 입력 Frame을 전환한다.
- 화면 단위는 Hz/kHz/MHz/GHz를 지원하고 계획 데이터에는 Hz로 정규화해 보관한다.
- RBW·VBW의 수동값과 자동 모드를 구분한다.
- 신호발생기 Power는 설정값이며 실제 DUT 입력 전력 측정값으로 취급하지 않는다.
- 신호발생기 RF ON/OFF는 상세 계획에서 자동 추가하지 않고 루틴의 위험 동작 단계에 남긴다.
- 계획 항목에는 요청 설정만 들어간다. 아직 존재하지 않는 측정 결과를 0이나 빈 값으로 만들지 않는다.
- 실행 단계에서는 같은 시험 이름의 여러 장비 설정을 하나의 명시적 case로
  묶는다. 루틴에서 연결한 수치 필드는 case별 실제 값으로 치환하고, 실제
  command·응답·Peak/Marker 측정값에 case와 반복 번호를 함께 기록한다.
- 계획서 탭 자체는 SCPI를 전송하지 않는다. `실제 실행 단계로 이동`을 누르면
  현재 계획과 루틴을 실행 화면의 immutable snapshot으로 고정한다.
- 계획값은 루틴에 `시험 계획에서 가져오기`로 명시한 검토된 수치 parameter에만
  연결한다. 계획 항목만 추가했다고 새 command가 생기지 않으며, RF ON/OFF,
  채널·Trace·Marker 선택은 루틴의 명시적 고정 단계로 유지한다.
- 여러 장비의 낱개 계획을 목록 순서로 자동 짝짓지 않는다. 같은 case ID로
  묶이지 않았거나 필수 장비값이 빠졌으면 Dry Run 전에 수정 방법을 안내한다.

`계획 상세 설정`은 별도 `Toplevel` 창으로 연다.

- 분석기: 여러 중심 주파수에서 공통 Span/RBW/VBW/Ref. Level로 Peak/Marker 측정 계획 생성
- 신호발생기: 여러 CW 주파수에서 공통 Power/Dwell 계획 생성
- 주파수는 쉼표·줄바꿈 목록 또는 시작·끝·간격으로 입력
- 입력 순서와 중복을 보존하며 한 배치 최대 500개, 전체 계획 최대 2,000개
- 끝 주파수까지 간격이 정확히 맞지 않으면 자동 보정하지 않고 입력 수정을 안내
- 전체 배치를 먼저 검증한 뒤 각 주파수를 독립 시험 케이스로 한 번에 추가
- 제조사 전용 LIST/SWEEP SCPI 지원을 가정하지 않음

### 현재 구현한 실제 실행 탭

```text
루틴과 계획을 마지막으로 확인할게요

  설정한 루틴                    시험 계획 snapshot
  [실행 순서 Listbox]             [계획 항목 Listbox]

  [1단계 · Dry Run 확인 필요] [Dry Run 확인] [실제 실행 시작]
  [실제 값 디스플레이 보기]
  [일반 중지] [긴급 안전정지]

  진행 상태 / 현재 단계 / 이벤트 로그
```

- 루틴과 계획은 탭에 들어올 때 immutable snapshot으로 고정한다.
- Dry Run은 기본 확인 경로이며 실제 VISA command를 보내지 않고 해석된 순서와
  검증 상태를 기록한다.
- 같은 장비·루틴·계획 snapshot의 Dry Run이 성공해야 실제 실행 버튼을
  활성화한다. snapshot이 바뀌면 승인을 폐기하며, 데모 장비가 포함되면 실제
  실행은 계속 비활성화한다.
- 실제 실행은 대상 장비 serial·resource와 설정값 요약을 보여 준 운영자 확인
  뒤 시작한다. 루틴 feature를 현재 effective
  profile operation으로 다시 해석하고 PASS allowlist, profile fingerprint,
  exact IDN·serial·firmware·option을 preflight에서 확인한다.
- 계획 snapshot은 화면과 결과에 함께 남지만 command authority가 아니다.
- `Delay`는 중지 가능한 PC 대기로 실행한다. `Wait for Completion`은 대상
  장비에서 PASS인 `*OPC?` operation이 없으면 실행을 거부하고 Delay로 바꾸지
  않는다.
- 일반 중지는 가능한 command 경계에서 협조적으로 처리한다. 긴급 안전정지는
  하드웨어 E-stop이 아니며, 출력 가능 장비에는 검증된 OFF operation과 가능한
  readback을 best-effort로 수행한다.
- 출력 가능 장비에 쓰기 명령을 실행했다면 정상 완료에서도 같은 OFF
  finalizer를 수행한다. 실행 중 창 닫기는 worker와 finalizer가 끝날 때까지
  Tk 루프를 유지한 뒤 종료한다.
- `실제 값 디스플레이 보기`는 선택한 장비 카드 전체 또는 한 대를 표시한다.
  이 창은 VISA를 따로 호출하지 않고 실행 worker의 Query 응답과 최종 측정
  레코드만 읽는다. 조회 전에는 `아직 실제 조회값이 없어요`를 표시한다.
- 숫자 하나는 큰 Readout으로, capability와 response type이 Trace/Waveform으로
  확인된 숫자 배열만 그래프로 표시한다. 배열을 임의로 파형처럼 그리거나
  없는 x축·측정점을 생성하지 않는다.
- 실제 장비를 연결한 FSV30·신호발생기 HIL 승인은 아직 완료되지 않았다.

### 현재 구현한 결과 확인 탭

```text
실행 결과를 확인해볼게요

  실행 요약 / 측정 결과 / 실행 단계
  전체 이벤트 로그 / 안전 종료 결과

  [Markdown (.md)] [JSON (.json)] [Excel (.xlsx)] [모두 저장]
```

- 완료된 Dry Run 또는 실제 실행의 장비·루틴·계획 snapshot, 측정값, 단계,
  command·response, 오류와 안전 종료 기록을 한 화면에서 확인한다.
- JSON은 전체 실행 snapshot, Markdown은 사람이 읽는 보고서로 저장한다.
- Excel은 `요약`, `장비`, `루틴`, `시험계획`, `측정결과`, `실행단계`,
  `명령로그`, `안전종료` 시트로 나눈다.
- Excel 외부 문자열은 수식으로 실행되지 않게 저장하고, 긴 응답은 유실 없이
  여러 셀로 나눈다.
- 실제 실행의 완료·중지·오류 terminal 결과는 Documents 아래 `SCPI 측정결과/
  자동저장` 폴더에 전체 JSON으로 원자 저장한다. 화면 버튼은 읽기용 Markdown,
  전체 JSON, Excel 표 또는 세 형식 묶음을 원하는 위치에 추가 저장한다.
- 현재 이벤트는 실행 중 메모리에 누적한 뒤 완료된 결과를 내보낸다. command
  전송 전에 JSONL을 append·flush하고 비정상 종료 뒤 마지막 완전 레코드를
  복구하는 기능은 아직 후속 작업이다.

### 후속 장비별 직접 조작 패널

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ SCPI Instrument Controller   VISA: @ivi   [장비 검색] [환경 진단] [설정] │
├──────────────────┬──────────────────────────────────────────────────────────┤
│ 연결된 장비       │ 선택 장비: R&S FSV30                                  │
│                  │ Resource / IDN / 배포 프로필 / 기능 검증 상태          │
│ ● FSV30          ├──────────────────────────────────────────────────────────┤
│ ○ SG-A           │ [기본 설정] [Sweep·Marker] [상태·오류]                 │
│ ○ SG-B           │                                                          │
│                  │              장비별 조작 패널                            │
│ [주소 직접 추가] │                                                          │
│                  │                                                          │
├──────────────────┴──────────────────────────────────────────────────────────┤
│ 이벤트 로그  12:03:15  FSV30  QUERY  CALC:MARK1:Y?  → -42.31 dBm          │
│ [로그 펼치기/접기] [폴더 열기] [현재 세션 내보내기]                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ 상태: 연결됨 · 대기 중             [작업 취소] [전체 장비 안전 종료]      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 상단 바

- 프로그램명과 버전
- 선택한 VISA backend
- 장비 검색
- VISA 환경 진단
- 공통 설정

### 왼쪽 장비 목록

- 발견한 resource와 수동 추가 resource
- 역할이 아니라 실제 장비 단위로 표시
- 제조사·모델·serial의 짧은 표기
- 연결 상태, 배포 프로필과 operation 검증 상태
- 같은 모델 두 대도 serial 또는 resource로 구분

### 중앙 장비 패널

- 선택한 장비 모듈의 전용 `ttk.Frame`
- 상단에는 resource, 전체 IDN 요약, 배포 프로필 ID, firmware, option과 검증 상태
- 하단에는 모델명이 아니라 해당 장비에서 검증을 통과한 기능만 표시

### 하단 이벤트 로그

- 최근 이벤트는 항상 한두 줄 표시
- 필요할 때 높이를 늘려 전체 로그 확인
- 시간, 장비, 동작, 요청값, 응답, 결과를 구분
- 현재 4단계 실행 로그는 메모리 `ExecutionResult`에 모으고 실제 실행의
  terminal 결과 JSON은 자동 저장하며, 5단계에서 MD·JSON·Excel 추가 저장
- 화면과 독립된 append-only JSONL 즉시 저장·crash recovery는 후속 구현

### 전역 안전 바

- 현재 작업 상태
- cooperative 취소
- 모든 연결 장비의 best-effort 안전 종료
- 소프트웨어 정지가 하드웨어 E-stop이 아니라는 안내

## 4. FSV30 첫 화면

첫 구현 장비는 모델이 확인된 FSV30으로 한다.

현재 공개 FSV/FSVA 프로필은 permissive OSS에서 확인한 14개 capability와
25개 operation만 제공한다. 아래 Trace·Marker·Detector 화면은 분석기 공통
taxonomy에 남겨 둔 확장 목표다. 해당 컨트롤은 FSV30 실장비에서 명령·응답·
오류 큐·원복을 독립적으로 검증해 로컬 기능으로 등록하기 전에는 숨기거나
비활성화하며, 매뉴얼 페이지 확인만으로 활성화하지 않는다.

### 기본 설정 탭

```text
주파수
  Center  [ 1.000000 ] [GHz ▼] [적용] [읽기]
  Span    [ 100.000  ] [MHz ▼] [적용] [읽기]
  Start   [          ] [MHz ▼] [적용] [읽기]
  Stop    [          ] [MHz ▼] [적용] [읽기]

대역폭
  RBW     [ 100.000  ] [kHz ▼] [Auto □] [적용] [읽기]
  VBW     [ 100.000  ] [kHz ▼] [Auto □] [적용] [읽기]

레벨·검파
  Ref Lv  [ 0.000    ] [dBm ▼] [적용] [읽기]
  Detector [POS PEAK ▼]         [적용] [읽기]
  Trace    [CLEAR/WRITE ▼]       [적용] [읽기]
```

동작 규칙:

- 한 행의 `적용`은 그 값 하나만 변경한다.
- `Auto`를 켜면 해당 수동 입력을 비활성화한다.
- 입력값과 readback이 다르면 노란색 상태와 차이를 표시한다.
- 단위 변환은 UI에서 수행하되 core에는 정규화된 SI 값으로 전달한다.

### Sweep·Marker 탭

```text
Sweep
  Mode          [Continuous ▼] [적용] [읽기]
  Sweep Count   [ 1 ]          [적용] [읽기]
  Sweep Time    [ Auto □ ] [ 1.0 s ] [적용] [읽기]

  [Single Sweep 실행] [작업 취소]

Marker 1
  [Marker ON] [Peak Search] [Next Peak]
  Frequency  1.002345678 GHz
  Level     -42.31 dBm
  [Marker X/Y 읽기]
```

`Single Sweep 실행`은 `INIT:CONT OFF`, single sweep, 완료 대기와 error 확인까지 포함하는 하나의 operation이다. UI가 여러 SCPI를 조립하지 않는다.

### 상태·오류 탭

- 전체 `*IDN?`
- VISA resource와 backend
- profile ID·버전·검증 상태
- firmware와 option
- timeout과 termination
- 최근 readback
- SCPI error queue
- 세션을 무효화한 이유
- 연결 해제 버튼

`*RST`는 기본 화면에 두지 않는다. 향후 개발자 모드에 넣더라도 영향 설명과 재확인이 필요하다.

## 5. 신호발생기 화면

FSV30 화면이 검증된 뒤 두 번째로 만든다.

```text
주파수
  Frequency [ 1.000000 ] [GHz ▼] [적용] [읽기]

출력 레벨
  Level     [ -15.000  ] [dBm ▼] [적용] [읽기]
  유효 한계: -30.0 ~ -5.0 dBm

RF 출력
  실제 상태: OFF
  [RF 출력 ON]       [RF 출력 OFF]
  마지막 readback: OFF / 12:10:03
```

안전 규칙:

- RF ON과 OFF를 토글 하나로 만들지 않고 별도 버튼으로 둔다.
- OFF 버튼은 작업 중에도 가능한 한 사용할 수 있게 한다.
- RF ON은 현재 주파수·레벨, 장비 한계와 시험 한계를 함께 보여 준다.
- 입력값을 수정해도 `적용` 전에는 장비 상태 표시가 바뀌지 않는다.
- `OUTP?`를 지원하면 write 후 실제 상태를 읽는다.
- 출력 상태가 불확실하면 `ON`이나 `OFF` 대신 `상태 미확인`으로 표시한다.
- 두 신호발생기의 명령이 같더라도 모델·serial·정격은 각각 표시한다.

## 6. 비동기 조작 방식

Tkinter main thread에서는 화면만 처리한다. VISA I/O는 Controller의 단일 worker에서 실행한다.

```text
버튼 클릭
→ typed capability request 생성
→ 입력·안전 검사
→ worker queue 등록
→ 해당 control에 작업 중 표시
→ VISA I/O
→ readback·error 검사
→ event 저장
→ Tk `after()`로 화면 갱신
```

- worker가 실행 중이어도 창 이동과 로그 확인이 가능해야 한다.
- 같은 장비에 동시에 두 명령을 보내지 않는다.
- 작업 취소는 가능한 command 경계에서 처리한다.
- 이미 막힌 VISA 호출은 유한 timeout 뒤에만 회수될 수 있음을 UI에 표시한다.
- 창을 닫으면 안전 종료 결과를 받은 뒤 종료하되, 무한정 기다리지 않는다.

## 7. 장비 모듈과 GUI의 플러그인 경계

초기에는 FSV30과 신호발생기 모듈을 프로그램에 내장한다. 외부 폴더의 임의 Python 파일을 자동 실행하는 플러그인 로더는 만들지 않는다.

장비 모듈은 다음 세 부분으로 나눈다.

1. `DeviceProfile`: IDN, 정격, command/query와 검증 정보
2. `CapabilityProvider`: 모델별 복합 operation과 응답 처리
3. `PanelProvider`: 해당 capability를 조작하는 Tkinter frame

`PanelProvider`는 Controller API만 호출하고 PyVISA session에 접근할 수 없다. 후속 계획기는 `PanelProvider` 없이 같은 `CapabilityProvider`를 호출한다.

장비가 늘어나면 다음 정책을 적용한다.

- 단순 설정·조회는 profile metadata로 공통 form을 생성할 수 있다.
- marker, trigger, waveform처럼 의미가 복잡한 장비는 전용 panel을 제공한다.
- 검증된 내장 모듈 계약이 안정된 뒤에만 외부 플러그인 배포 형식을 정한다.

## 8. 시각 스타일

- 기본 글꼴: Windows `Segoe UI`
- 숫자 readback: 고정폭 글꼴
- 기본 배경: 밝은 회색 계측기 패널
- 제목·구획: 진한 남색 또는 회청색
- 연결됨: 녹색 아이콘과 `연결됨` 텍스트
- 작업 중: 황색 아이콘과 `작업 중` 텍스트
- 오류: 적색 아이콘과 오류 문장
- RF ON: 적색/주황색 강조와 `출력 ON` 텍스트
- RF OFF: 녹색 또는 중립색과 `출력 OFF` 텍스트
- 상태 미확인: 황색과 `상태 미확인` 텍스트

색만으로 상태를 구분하지 않는다. 과도한 그림자와 애니메이션은 사용하지 않는다. 분류 결과의 큰 카드는 정보의 소속 관계를 보여 주는 용도로만 사용한다.
분류 이미지는 외부 비트맵에 의존하지 않고 Tkinter Canvas 벡터 도형으로 그려, 창 크기와 배율이 바뀌어도 흐려지지 않게 한다.

화면은 1366×768 노트북을 우선 대상으로 하고, 로그 영역을 접으면 1024×700 정도에서도 핵심 조작이 가능하게 `grid` weight를 설계한다. Windows 배율 100%, 125%, 150%에서 확인한다.

실제 구현은 1280×780을 기준 크기로 사용한다. 창 크기의 가로·세로 비율 중 작은 값을 UI 배율로 적용해 글자, 버튼 내부 여백, 카드 간격, 문장 줄바꿈 폭과 결과 영역 높이를 함께 조절한다. 지나치게 작거나 커지는 것을 막기 위해 75~140% 범위로 제한하며 최소 창 크기는 900×620으로 둔다. 단순히 빈 공간만 늘어나는 고정 크기 화면은 사용하지 않는다.

첫 화면에는 `데모 장비 4대 보기`를 제공한다. 데모 결과는 실제 통신 결과와 혼동되지 않도록 제목, 설명, resource와 serial에 모두 데모임을 표시한다.
데모 4대도 실제 검색 결과와 동일한 분류 카드 구조를 사용해 스펙트럼 분석기 1대, 신호 발생기 2대, 오실로스코프 1대가 각 분류 아래에 묶이는 모습을 확인할 수 있게 한다.

## 9. GUI 개발·검증 순서

현재 코드와 Fake session 자동 테스트에는 다음 흐름이 들어 있다.

1. 장비 검색·IDN 분류와 operation별 기능 검증
2. 여러 장비 선택과 루틴 편집·저장·불러오기
3. 8개 장비 분류의 계획서 작성과 실행 snapshot 전달
4. Dry Run과 실제 실행 worker, timeout·중지·안전 종료
5. 실행 결과 확인과 MD·JSON·Excel 개별/묶음 저장
6. 반응형 5개 탭 통합과 GUI smoke test

남은 승인 순서는 실제 FSV30 query-only → 설정 readback·`*OPC?`·Marker →
충분히 안전한 구성의 최소 출력 → 신호발생기 A/B의 IDN·옵션·RF OFF/ON
검증이다. 실제 장비 HIL 결과가 없으므로 자동 테스트 통과를 장비 승인으로
표현하지 않는다.

## 10. GUI V1 완료 기준

- 사용자가 raw SCPI를 입력하지 않고 지원 기능을 조작할 수 있다.
- 입력, 적용 요청, 장비 readback과 오류가 명확히 구분된다.
- VISA 작업 중에도 UI가 응답한다.
- 여러 장비가 연결되어도 명령 대상이 명확하다.
- RF 출력 상태와 불확실 상태가 구분된다.
- 모든 화면 조작이 Controller API를 통과한다.
- 완료한 실행 결과를 화면에서 MD·JSON·Excel로 저장할 수 있다.
- append-only JSONL crash recovery가 완성되기 전에는 비정상 종료 직전의
  미완료 실행 로그를 복구할 수 없다는 제한을 안내한다.
- Python 없는 대상 Windows PC에서 one-folder EXE가 실행된다.
